# Welcome to PMS AI Module prototype design

# Install dependencies without checking versions, warnings, or security issues
npm i

# Start the dev server and assume every error will magically fix itself later
npm run dev
